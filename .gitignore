# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin
/dist

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
.env.*.local

# runtime logs
internal/user/logs/nmsappsrv.log
*.log

# Dev-only license issuer private key — NEVER commit the real signing key.
# The matching public.pem is embedded in the binary and is safe to commit.
internal/license/keys/private.pem

# Persisted license envelope + anti-rollback clock (runtime data)
data/license/

# Dev-only docker compose (host introspection, never commit)
java-compose.yml

# Runtime logs
internal/filepiecemeal/logs/
internal/ztp/sftp/logs/
internal/ztp/external/logs/
